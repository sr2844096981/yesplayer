const state = {
    // 当前激活菜单项
    // activeMenu: '/home'
}

const mutations = {
    /*     setActiveMenu(state, activeMenu) {
            state.activeMenu = activeMenu
        } */
}

const actions = {

}

export default {
    namespaced: true,
    state,
    mutations,
    actions
}